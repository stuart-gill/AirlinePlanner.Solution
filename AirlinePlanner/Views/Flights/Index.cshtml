<h1>Flights</h1>

@if (@Model.Count == 0)
{
  <h3>No flights have been added yet!</h3>
}

@foreach (var city in Model)
{
  <h3><a href='/flights/@flight.GetId()/flights'>@flight.GetName(), @flight.GetDepartTime(), @flight.GetStatus()</a></h3>
}

<p><a href='/'>Back home</a></p>
<p><a href='/flights/new'>Create a new Flight</a></p>